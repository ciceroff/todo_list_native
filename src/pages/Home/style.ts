import {StyleSheet} from 'react-native';
import {globalColors} from '../../styles/global';

export const styles = StyleSheet.create({
  body: {backgroundColor: globalColors['gray-600'], padding: 20},
});
